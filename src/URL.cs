namespace URLStandard;

public class URL
{

  // Ref: https://url.spec.whatwg.org/#dom-url-url
  public URL(string url, string baseURL = null)
  {
    URLRecord? parsedBase = null;
    if (baseURL is not null)
    {
      parsedBase = null;
    }
  }
}